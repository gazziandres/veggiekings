<% if @orders.count > 0 %>
  <div class="container">
    <table class="table">
      <thead>
        <tr>
          <th>Plato</th>
          <th>Valor</th>
          <th>Cantidad</th>
          <th>Subtotal</th>
          <th>Estado</th>
        </tr>
      </thead>
      <tbody>
        <% @orders.each do |order| %>
          <tr>
            <td> <%= order.plato.name %> </td>
            <td> <%= order.price %></td>
            <td> <%= order.quantity %> </td>
            <td> <%= order.price * order.quantity %></td>
            <td> <%= order.payed == true ? 'Pagado' : 'Sin Cancelar' %></td>
          </tr>
        <% end %>
        <tr class="table-dark">
        <td></td>
        <td></td>
        <td><strong>Total de la compra:</strong></td>
        <td><strong><%= @total %></strong></td>
        </tr>
      </tbody>
    </table>

    <%= link_to 'Limpiar carro', clean_orders_path, class: 'btn btn-outline-danger'  %>
    <%= link_to 'Confirmar compra', orders_address_path, class: 'btn btn-success float-right' %>
    <!-- <%= link_to 'Confirmar compra', pre_pay_billings_path, class: 'btn btn-success float-right' %> -->
  </div>
  <br><br><br><br><br><br><br><br><br><br><br>
  <% else %>
    <div class="">
      <div class="jumbotron">
        <h3>Aún no has seleccionado productos</h3>
        <p>Si no te parecen adecuados los platos para la selección de hoy, prueba creando tu propia mezcla de ensaladas</p>
      </div>
    </div>
    <div class="text-center">
      <img src="http://www.cuentoscortos.com/imagenes/1123-grande" alt="Sin seleccionar">
    </div>
  <% end %>
